# 2026-05-14 変更ログ

## 11:14 - モバイル用ボトムナビゲーションを実装（Issue #4 対応）

**種別**: 追加・編集  
**対象ファイル**:
- `frontend/src/components/layout/BottomNav.tsx`（新規）
- `frontend/src/components/layout/AppShell.tsx`
- `frontend/src/components/layout/Sidebar.tsx`
- `frontend/src/pages/HomePage.tsx`

### 変更内容
- `BottomNav.tsx` を新規作成：モバイル画面（md未満）の下部に固定表示されるナビゲーションバー
  - 「グループ一覧」リンク、ユーザーアバター/名前、ログアウトボタンを配置
  - `md:hidden` でデスクトップでは非表示
- `AppShell.tsx` にモバイル用トップヘッダー（🗓 GroupSync）と `BottomNav` を組み込み
  - モバイルでコンテンツ下部にボトムナビ分の余白（`pb-20`）を追加
  - メインコンテンツのパディングをモバイルで `p-4`、デスクトップで `p-8` に調整
- `Sidebar.tsx` をモバイルで非表示に（`hidden md:flex`）
- `HomePage.tsx` のボタン配置をモバイルで縦並び、sm以上で横並びに変更

### 理由
Issue #4 のスマホ対応として、モバイルではサイドバーの代わりにボトムナビゲーションを表示する方針で実装。
