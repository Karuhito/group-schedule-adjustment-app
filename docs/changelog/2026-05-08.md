## 外部サービスのセットアップ・接続確認

**種別**: 設定 / 修正
**対象ファイル**: `backend/app/models/models.py`, `backend/alembic/versions/0001_initial_tables.py`

### 変更内容
- Supabase プロジェクトを作成し `backend/.env` に `DATABASE_URL` を設定（Session Pooler URL: `aws-1-ap-northeast-1.pooler.supabase.com:5432`）
- Discord Developer Portal でアプリを作成し `DISCORD_CLIENT_ID` / `DISCORD_CLIENT_SECRET` を `.env` に設定
- `JWT_SECRET`, `DISCORD_REDIRECT_URI`, `FRONTEND_URL`, `APP_ENV` を `.env` に設定
- `alembic upgrade head` で Supabase に初期テーブル（users, groups, group_members, schedules）を作成
- `backend/app/models/models.py`: 全 `DateTime` カラムを `DateTime(timezone=True)` に変更（TIMESTAMPTZ）
- `backend/alembic/versions/0001_initial_tables.py`: 同様に全 `DateTime()` を `DateTime(timezone=True)` に変更
- Discord OAuth フローの動作確認・Supabase の users テーブルへのユーザー登録を確認

### 理由
Supabase への INSERT 時に `asyncpg.exceptions.DataError: can't subtract offset-naive and offset-aware datetimes` エラーが発生。タイムゾーン付き datetime（`datetime.now(timezone.utc)`）をタイムゾーンなしの `TIMESTAMP` カラムに挿入しようとしていたため。`TIMESTAMP WITH TIME ZONE`（`DateTime(timezone=True)`）に統一して解決した。

---

## 14:30 - グループ用 Pydantic スキーマの追加

**種別**: 追加
**対象ファイル**: `backend/app/schemas/schemas.py`

### 変更内容
- `GroupCreate`: グループ作成リクエスト用スキーマ（name フィールド）
- `GroupResponse`: グループ情報レスポンス用スキーマ（id, name, invite_code, member_count, is_owner）
- `GroupPreviewResponse`: グループ一覧表示用スキーマ（id, name, member_count）
- `MemberResponse`: グループメンバー情報用スキーマ（user_id, username, avatar_url, is_owner）

### 理由
Groups API 実装計画のStep 1として、グループ管理に必要な4つの Pydantic スキーマを追加。APIエンドポイント実装の基盤となる。

---

## 15:45 - グループルーター骨格 + main.py 登録

**種別**: 追加
**対象ファイル**: `backend/app/routers/groups.py`, `backend/app/main.py`

### 変更内容
- `backend/app/routers/groups.py` を新規作成
  - APIRouter プリフィックス: `/groups`
  - 8つのエンドポイント（全て NotImplemented スタブ）：
    - `POST /`: グループ作成
    - `GET /`: グループ一覧取得
    - `GET /by-code/{invite_code}`: グループプレビュー
    - `POST /by-code/{invite_code}/join`: グループ参加
    - `GET /{group_id}/members`: メンバー一覧取得
    - `DELETE /{group_id}`: グループ削除
    - `DELETE /{group_id}/members/{user_id}`: メンバー削除
  - ヘルパー関数 `_generate_unique_invite_code()` を実装
- `backend/app/main.py` を修正
  - `groups` ルーターをインポート
  - `app.include_router(groups.router)` で groups ルーターを登録

### 理由
Groups API 実装計画のTask 2として、グループ管理エンドポイント全体の骨格を作成。後続タスク（Task 4-10）でエンドポイントを実装する際の基盤となる。

---

## 16:30 - conftest.py にグループテスト用フィクスチャを追加

**種別**: 修正
**対象ファイル**: `backend/tests/conftest.py`

### 変更内容
- `test_user` フィクスチャを追加：グループテスト用のオーナーユーザーを DB に作成
- `other_user` フィクスチャを追加：グループテスト用の別ユーザー（メンバー）を DB に作成
- 既存の `db_session` と `client` フィクスチャは保持
- import 文を整理し、User モデルを import

### 理由
Groups API 実装計画のTask 3として、後続のグループAPIテスト（Task 4-10）で使用するテストフィクスチャを conftest.py に追加。`_make_jwt` はテストファイル（test_groups.py）に直接定義するため conftest には追加しない（既存の test_auth.py と同じパターン）。

---

## 17:00 - POST /groups（グループ作成）を TDD で実装

**種別**: 追加
**対象ファイル**: `backend/tests/test_groups.py`, `backend/app/routers/groups.py`

### 変更内容
- `backend/tests/test_groups.py` を新規作成
  - `test_create_group`: 認証済みユーザーがグループを作成できることを検証
  - `test_create_group_unauthenticated`: 未認証ユーザーが 401 を受けることを検証
- `backend/app/routers/groups.py` の `create_group` スタブを実装
  - `_generate_unique_invite_code()` で 8 文字の招待コードを生成
  - `Group` モデルと `GroupMember` モデルを DB に保存
  - `GroupResponse` を返す（member_count=1, is_owner=True）

### 理由
Groups API 実装計画の Task 4 として、TDD（Red → Green）サイクルで `POST /groups` エンドポイントを実装。テスト 2 件全て PASSED を確認。

---

## 17:30 - GET /groups（グループ一覧）を TDD で実装

**種別**: 追加
**対象ファイル**: `backend/tests/test_groups.py`, `backend/app/routers/groups.py`

### 変更内容
- `backend/tests/test_groups.py` にテスト 3 件を追加
  - `test_list_groups_empty`: グループがない場合に空配列が返ることを検証
  - `test_list_groups`: 自分が作成した 2 グループが取得できることを検証（is_owner, member_count も確認）
  - `test_list_groups_only_mine`: 他ユーザーのグループが一覧に含まれないことを検証
- `backend/app/routers/groups.py` の `list_groups` スタブを実装
  - `GroupMember` を JOIN して自分が参加しているグループのみ取得
  - `func.count(GroupMember.id)` で `member_count` を集計
  - `group.created_by == current_user.id` で `is_owner` を判定

### 理由
Groups API 実装計画の Task 5 として、TDD（Red → Green）サイクルで `GET /groups` エンドポイントを実装。テスト 5 件全て PASSED を確認。

---

## 18:00 - GET /groups/by-code/{invite_code}（招待コードプレビュー）を TDD で実装

**種別**: 追加
**対象ファイル**: `backend/tests/test_groups.py`, `backend/app/routers/groups.py`

### 変更内容
- `backend/tests/test_groups.py` にテスト 2 件を追加
  - `test_preview_group`: 招待コードでグループ情報（name, member_count）が取得でき、invite_code が含まれないことを検証
  - `test_preview_group_not_found`: 存在しない招待コードで 404 が返ることを検証
- `backend/app/routers/groups.py` の `preview_group` スタブを実装
  - `Group.invite_code` で検索し、見つからない場合は 404 を返す
  - `func.count(GroupMember.id)` で `member_count` を集計
  - `GroupPreviewResponse`（id, name, member_count のみ）を返す

### 理由
Groups API 実装計画の Task 6 として、TDD（Red → Green）サイクルで `GET /groups/by-code/{invite_code}` エンドポイントを実装。テスト 7 件全て PASSED を確認。

---

## 18:30 - POST /groups/by-code/{invite_code}/join（グループ参加）を TDD で実装

**種別**: 追加
**対象ファイル**: `backend/tests/test_groups.py`, `backend/app/routers/groups.py`

### 変更内容
- `backend/tests/test_groups.py` にテスト 3 件を追加
  - `test_join_group`: 招待コードでグループに参加でき、member_count=2・is_owner=False になることを検証
  - `test_join_group_already_member`: 既にメンバーのグループへ再参加しようとすると 409 が返ることを検証
  - `test_join_group_not_found`: 存在しない招待コードで 404 が返ることを検証
- `backend/app/routers/groups.py` の `join_group` スタブを実装
  - `Group.invite_code` で検索し、見つからない場合は 404 を返す
  - `GroupMember` の重複チェックを行い、既存メンバーの場合は 409 を返す
  - `GroupMember` を DB に追加し `commit()` 後に COUNT クエリで `member_count` を取得
  - `GroupResponse` を返す（`group.created_by == current_user.id` で `is_owner` を判定）

### 理由
Groups API 実装計画の Task 7 として、TDD（Red → Green）サイクルで `POST /groups/by-code/{invite_code}/join` エンドポイントを実装。テスト 10 件全て PASSED を確認。

---

## 19:00 - GET /groups/{group_id}/members（メンバー一覧）を TDD で実装

**種別**: 追加
**対象ファイル**: `backend/tests/test_groups.py`, `backend/app/routers/groups.py`

### 変更内容
- `backend/tests/test_groups.py` にテスト 3 件を追加
  - `test_list_members`: グループメンバー一覧が取得でき、is_owner・username が正しいことを検証
  - `test_list_members_not_member`: 非メンバーがアクセスすると 403 が返ることを検証
  - `test_list_members_group_not_found`: 存在しない group_id で 404 が返ることを検証
- `backend/app/routers/groups.py` の `list_members` スタブを実装
  - グループ存在確認（404）→ メンバーシップ確認（403）→ メンバー一覧取得の順で処理
  - `User` と `GroupMember` を JOIN して全メンバーを取得
  - `group.created_by == user.id` で `is_owner` を判定し `MemberResponse` を返す

### 理由
Groups API 実装計画の Task 8 として、TDD（Red → Green）サイクルで `GET /groups/{group_id}/members` エンドポイントを実装。テスト 13 件全て PASSED を確認。

---

## 19:30 - DELETE /groups/{group_id}（グループ削除）を TDD で実装

**種別**: 追加
**対象ファイル**: `backend/tests/test_groups.py`, `backend/app/routers/groups.py`

### 変更内容
- `backend/tests/test_groups.py` にテスト 3 件を追加
  - `test_delete_group`: オーナーがグループを削除でき（204）、削除後に一覧から消えることを検証
  - `test_delete_group_not_owner`: オーナー以外が削除しようとすると 403 が返ることを検証
  - `test_delete_group_not_found`: 存在しない group_id で 404 が返ることを検証
- `backend/app/routers/groups.py` の `delete_group` スタブを実装
  - グループ存在確認（404）→ オーナー確認（403）→ 関連データ削除の順で処理
  - 外部キー制約を守るため schedules → group_members → groups の順で削除
  - `db.commit()` で確定後 None を返す（status_code=204）

### 理由
Groups API 実装計画の Task 9 として、TDD（Red → Green）サイクルで `DELETE /groups/{group_id}` エンドポイントを実装。テスト 16 件全て PASSED を確認。

---

## 20:00 - DELETE /groups/{group_id}/members/{user_id}（メンバー追い出し）を TDD で実装

**種別**: 追加
**対象ファイル**: `backend/tests/test_groups.py`, `backend/app/routers/groups.py`

### 変更内容
- `backend/tests/test_groups.py` にテスト 4 件を追加
  - `test_remove_member`: オーナーがメンバーを追い出せ（204）、追い出し後にメンバーが1人になることを検証
  - `test_remove_self_as_owner`: オーナーが自分自身を追い出そうとすると 400 が返ることを検証
  - `test_remove_member_not_owner`: オーナー以外がメンバーを追い出そうとすると 403 が返ることを検証
  - `test_remove_member_group_not_found`: 存在しない group_id で 404 が返ることを検証
- `backend/app/routers/groups.py` の `remove_member` スタブを実装
  - グループ存在確認（404）→ オーナー確認（403）→ 自己追い出し禁止（400）→ 削除の順で処理
  - schedules（group_id + user_id）→ group_members（group_id + user_id）の順で削除
  - `db.commit()` で確定後 None を返す（status_code=204）

### 理由
Groups API 実装計画の Task 10 として、TDD（Red → Green）サイクルで `DELETE /groups/{group_id}/members/{user_id}` エンドポイントを実装。テスト 20 件全て PASSED を確認。
