## 外部サービスのセットアップ・接続確認

**種別**: 設定 / 修正
**対象ファイル**: `backend/app/models/models.py`, `backend/alembic/versions/0001_initial_tables.py`

### 変更内容
- Supabase プロジェクトを作成し `backend/.env` に `DATABASE_URL` を設定（Session Pooler URL: `aws-1-ap-northeast-1.pooler.supabase.com:5432`）
- Discord Developer Portal でアプリを作成し `DISCORD_CLIENT_ID` / `DISCORD_CLIENT_SECRET` を `.env` に設定
- `JWT_SECRET`, `DISCORD_REDIRECT_URI`, `FRONTEND_URL`, `APP_ENV` を `.env` に設定
- `alembic upgrade head` で Supabase に初期テーブル（users, groups, group_members, schedules）を作成
- `backend/app/models/models.py`: 全 `DateTime` カラムを `DateTime(timezone=True)` に変更（TIMESTAMPTZ）
- `backend/alembic/versions/0001_initial_tables.py`: 同様に全 `DateTime()` を `DateTime(timezone=True)` に変更
- Discord OAuth フローの動作確認・Supabase の users テーブルへのユーザー登録を確認

### 理由
Supabase への INSERT 時に `asyncpg.exceptions.DataError: can't subtract offset-naive and offset-aware datetimes` エラーが発生。タイムゾーン付き datetime（`datetime.now(timezone.utc)`）をタイムゾーンなしの `TIMESTAMP` カラムに挿入しようとしていたため。`TIMESTAMP WITH TIME ZONE`（`DateTime(timezone=True)`）に統一して解決した。

---

## 14:30 - グループ用 Pydantic スキーマの追加

**種別**: 追加
**対象ファイル**: `backend/app/schemas/schemas.py`

### 変更内容
- `GroupCreate`: グループ作成リクエスト用スキーマ（name フィールド）
- `GroupResponse`: グループ情報レスポンス用スキーマ（id, name, invite_code, member_count, is_owner）
- `GroupPreviewResponse`: グループ一覧表示用スキーマ（id, name, member_count）
- `MemberResponse`: グループメンバー情報用スキーマ（user_id, username, avatar_url, is_owner）

### 理由
Groups API 実装計画のStep 1として、グループ管理に必要な4つの Pydantic スキーマを追加。APIエンドポイント実装の基盤となる。

---

## 15:45 - グループルーター骨格 + main.py 登録

**種別**: 追加
**対象ファイル**: `backend/app/routers/groups.py`, `backend/app/main.py`

### 変更内容
- `backend/app/routers/groups.py` を新規作成
  - APIRouter プリフィックス: `/groups`
  - 8つのエンドポイント（全て NotImplemented スタブ）：
    - `POST /`: グループ作成
    - `GET /`: グループ一覧取得
    - `GET /by-code/{invite_code}`: グループプレビュー
    - `POST /by-code/{invite_code}/join`: グループ参加
    - `GET /{group_id}/members`: メンバー一覧取得
    - `DELETE /{group_id}`: グループ削除
    - `DELETE /{group_id}/members/{user_id}`: メンバー削除
  - ヘルパー関数 `_generate_unique_invite_code()` を実装
- `backend/app/main.py` を修正
  - `groups` ルーターをインポート
  - `app.include_router(groups.router)` で groups ルーターを登録

### 理由
Groups API 実装計画のTask 2として、グループ管理エンドポイント全体の骨格を作成。後続タスク（Task 4-10）でエンドポイントを実装する際の基盤となる。

---

## 16:30 - conftest.py にグループテスト用フィクスチャを追加

**種別**: 修正
**対象ファイル**: `backend/tests/conftest.py`

### 変更内容
- `test_user` フィクスチャを追加：グループテスト用のオーナーユーザーを DB に作成
- `other_user` フィクスチャを追加：グループテスト用の別ユーザー（メンバー）を DB に作成
- 既存の `db_session` と `client` フィクスチャは保持
- import 文を整理し、User モデルを import

### 理由
Groups API 実装計画のTask 3として、後続のグループAPIテスト（Task 4-10）で使用するテストフィクスチャを conftest.py に追加。`_make_jwt` はテストファイル（test_groups.py）に直接定義するため conftest には追加しない（既存の test_auth.py と同じパターン）。
