# 2026-05-09 作業ログ

---

## 午前 - スケジュール API ブレインストーミング・設計

**種別**: 追加
**対象ファイル**: `docs/superpowers/specs/2026-05-09-schedules-api-design.md`

### 変更内容
- スケジュール API の設計をブレインストーミングで確定
  - 一括置き換え（PUT）方式を採用（全削除→全挿入）
  - 自由時間帯（start_time / end_time）で登録する形式
  - GET は `end_time >= NOW()` で過去スロットを除外
  - `avatar_url` が null の場合は API がそのまま null を返す（フロント側でデフォルト画像を表示）
- 設計ドキュメントを `docs/superpowers/specs/2026-05-09-schedules-api-design.md` に保存

### 理由
Discord 以外の認証プロバイダーへの将来的な拡張を考慮し、バックエンドに Discord 固有のフォールバックロジックを持たせない設計にした。

---

## 午前 - スケジュール API 実装計画の作成

**種別**: 追加
**対象ファイル**: `docs/superpowers/plans/2026-05-09-schedules-api.md`

### 変更内容
- 5タスク構成の実装計画を作成
  - Task 1: Pydantic スキーマ追加
  - Task 2: スケジュールルーター骨格 + main.py 登録
  - Task 3: PUT エンドポイントを TDD で実装
  - Task 4: GET エンドポイントを TDD で実装
  - Task 5: 全テスト実行・変更ログ作成

### 理由
設計ドキュメントをもとに TDD・DRY・YAGNI の方針で実装計画を作成した。

---

## 21:18 - スケジュール API 実装

**種別**: 追加
**対象ファイル**: `backend/app/routers/schedules.py`, `backend/app/schemas/schemas.py`, `backend/app/main.py`, `backend/tests/test_schedules.py`

### 変更内容
- スケジュール用 Pydantic スキーマを追加（ScheduleSlot, ScheduleUpsert, ScheduleResponse, MemberScheduleResponse）
- `PUT /groups/{group_id}/schedules`: 自分の空き時間を一括登録（既存スロットを全削除→全挿入）
- `GET /groups/{group_id}/schedules`: グループ全メンバーの空き時間を取得（end_time >= NOW() でフィルタ）
- CORS の許可メソッドに `PUT` を追加
- テスト 10 件全て PASSED

### 理由
スケジュール調整アプリの中核機能。「空き時間を選択して送る」一括置き換えモデルを採用し、フロントのカレンダー UI と自然に対応する設計にした。avatar_url が null の場合はそのまま返し、デフォルト画像はフロント側で処理する。
