## 17:30 - 修正: スケジュールグリッドの表示範囲を「入力日〜6日後」に変更（Issue #11 対応）

**種別**: 修正
**対象ファイル**:
- `frontend/src/components/schedule/weeklyGridUtils.ts`（修正）
- `frontend/src/components/schedule/WeeklyGrid.tsx`（修正）
- `frontend/src/components/schedule/weeklyGridUtils.test.ts`（修正）
- `frontend/src/components/schedule/WeeklyGrid.test.tsx`（修正）

### 変更内容
- `getWeekDates` を「月曜起算の週」から「基準日から6日後」に変更
- `WeeklyGrid` の曜日名表示を固定インデックスから `Date.getDay()` ベースに変更（実際の曜日を表示）
- 対応するテストを新仕様に合わせて更新

### 理由
Issue #11 の要件対応。月曜〜日曜固定では、例えば金曜日に開いた場合に月〜木が過去日（disabled）になり入力できない問題を解消するため。

## 14:35 - SlotForm コンポーネントの削除

**種別**: 削除
**対象ファイル**: 
- `frontend/src/components/schedule/SlotForm.tsx`
- `frontend/src/components/schedule/SlotForm.test.tsx`

### 変更内容
- SlotForm コンポーネントと関連するテストファイルを削除

### 理由
ScheduleTab.tsx が WeeklyGrid に完全に置き換えられたため、SlotForm は不要になった。

## 12:08 - 追加: 週次グリッド型スケジュール入力UI（Issue #8 対応）

**種別**: 追加・修正・削除
**対象ファイル**:
- `frontend/src/components/schedule/WeeklyGrid.tsx`（新規）
- `frontend/src/components/schedule/WeeklyGrid.test.tsx`（新規）
- `frontend/src/components/schedule/weeklyGridUtils.ts`（新規）
- `frontend/src/components/schedule/weeklyGridUtils.test.ts`（新規）
- `frontend/src/components/schedule/ScheduleTab.tsx`（修正）
- `frontend/src/components/schedule/ScheduleTab.test.tsx`（修正）
- `frontend/src/components/schedule/SlotForm.tsx`（削除）
- `frontend/src/components/schedule/SlotForm.test.tsx`（削除）
- `frontend/vite.config.ts`（修正）

### 変更内容
- `SlotForm`（テキスト入力）を廃止し、`WeeklyGrid`（週次グリッド）に置換
- 直近1週間（基準日が属する週の月〜日）の空き時間を30分単位でクリック入力可能に
- 当週外の既存スロットはGET時に保持し、保存時にマージしてデータロスを防止
- ScheduleTabに「自分の予定を入力」「みんなの予定」のサブタブを追加
- vite.config.ts に TZ: 'Asia/Tokyo' を追加（CI環境との差異防止）

### 理由
Issue #8 の要件対応。操作性向上のため週次グリッドUIを採用。
