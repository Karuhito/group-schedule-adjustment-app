## 11:00 - weeklyGridUtils（週次グリッド変換ユーティリティ）を実装

**種別**: 追加
**対象ファイル**: `frontend/src/components/schedule/weeklyGridUtils.ts`, `frontend/src/components/schedule/weeklyGridUtils.test.ts`, `frontend/vite.config.ts`

### 変更内容
- `weeklyGridUtils.ts` を新規作成し、週次グリッドUIで使う変換ユーティリティ関数を実装
  - `getWeekDates`: 月曜起点で7日分のDateを返す
  - `isPastDate`: 今日より前の日付かどうかを判定する
  - `getTimeSlots`: 30分刻み48スロットの時刻文字列を返す
  - `keySetToSlots`: "YYYY-MM-DD HH:mm" キー集合を連続マージしてScheduleSlot[]に変換する
  - `slotsToKeySet`: ScheduleSlot[]を30分単位のキー集合に展開する
  - `partitionSlotsByWeek`: スロットを当週内・当週外に分類する
- `weeklyGridUtils.test.ts` を新規作成し、全関数のユニットテスト（14件）を実装（TDD）
- `vite.config.ts` に `/// <reference types="vitest" />` と `env: { TZ: 'Asia/Tokyo' }` を追加（CI環境との差異防止）

### 理由
週次グリッドUIコンポーネント実装の基盤となるユーティリティ関数群をTDDで先行実装するため

---

## 11:42 - weeklyGridUtils の23:30スロット逆転バグ・日跨ぎバグを修正

**種別**: 修正
**対象ファイル**: `frontend/src/components/schedule/weeklyGridUtils.ts`, `frontend/src/components/schedule/weeklyGridUtils.test.ts`

### 変更内容
- `keySetToSlots`: 時刻を文字列ではなく「分数（0-1440）」として扱うように変更し、`addMinutes` による `% 24` ラップアラウンドを排除。`makeSlot` の引数を分数に変更し、`Date` コンストラクタによる日跨ぎ自動処理を活用
- `addMinutes` ヘルパー関数を削除
- `makeSlot`: 引数を `startTime/endTime: string` から `startMinutes/endMinutes: number` に変更し、ミリ秒ベースで Date を計算
- `slotsToKeySet`: 日跨ぎスロット（`endMinutes <= startMinutes`）に対して `endMinutes += 24 * 60` の補正を追加し、ループ内で分が1440以上になった場合に翌日の dateStr を生成するよう修正
- バグ再現テスト2件を追加（`keySetToSlots` の23:30逆転テスト、`slotsToKeySet` の日跨ぎテスト）

### 理由
コードレビューで発見されたCritical/Highバグを修正するため。23:30セルの end_time が start_time より小さくなる逆転バグと、日跨ぎスロットのキーが生成されないバグを解消

---

## 11:49 - WeeklyGrid コンポーネントを実装（完全制御型）

**種別**: 追加
**対象ファイル**: `frontend/src/components/schedule/WeeklyGrid.tsx`, `frontend/src/components/schedule/WeeklyGrid.test.tsx`

### 変更内容
- `WeeklyGrid.test.tsx` を新規作成し、8件のテストを実装（TDD）
  - 7列ヘッダー（月〜日）の表示確認
  - 7×48=336個のセルボタン存在確認
  - 過去日セルのdisabled確認
  - 今日のセルのdisabled=false確認
  - クリック時のonToggle呼び出し確認（正しいキー形式）
  - selectedKeysに対応するaria-pressed=true確認
  - 月跨ぎ週のレンダリング確認
  - 当週外スロットが選択に反映されないことの確認
- `WeeklyGrid.tsx` を新規作成し、完全制御コンポーネントを実装
  - 内部状態（useState/useEffect）を持たない純粋な表示+イベント転送コンポーネント
  - セルのaria-labelは `"${月}月${日}日 ${時刻}"` 形式
  - 過去日セルはdisabled属性を付与
  - dateStringsとtodayDateStrをuseMemoでメモ化

### 理由
週次グリッドUIの完全制御コンポーネントをTDDで実装するため。親コンポーネントが状態を持ち、WeeklyGridは純粋な表示のみを担当する設計

---

## 11:58 - WeeklyGrid のアクセシビリティ改善とテスト修正

**種別**: 修正
**対象ファイル**: `frontend/src/components/schedule/WeeklyGrid.tsx`, `frontend/src/components/schedule/WeeklyGrid.test.tsx`

### 変更内容
- `WeeklyGrid.tsx`:
  - 左上の空 `<th>` に `scope="col"` を追加
  - 曜日列の `<th>` に `scope="col"` を追加
  - 時刻ラベルの `<td>` を `<th scope="row">` に変更
- `WeeklyGrid.test.tsx`:
  - 「当週外のスロットはセル選択に反映されない」テストを修正。空Setではなく当週外キー（`2026-06-01 14:00`）を含むSetを渡して実際にグリッドへの影響がないことを検証するように変更

### 理由
コードレビューで指摘されたWCAG 1.3.1違反（`<th>`にscope属性がない）を修正してスクリーンリーダー対応を改善するため。また当週外スロットテストが実質的な検証をしていなかったため、実際の動作を検証するテストに修正

---

## 12:02 - ScheduleTab に WeeklyGrid とサブタブを統合

**種別**: 修正
**対象ファイル**: `frontend/src/components/schedule/ScheduleTab.tsx`, `frontend/src/components/schedule/ScheduleTab.test.tsx`

### 変更内容
- `ScheduleTab.tsx` を完全書き換え
  - `SlotForm` を廃止し `WeeklyGrid`（完全制御コンポーネント）に置き換え
  - 「自分の予定を入力」「みんなの予定」のサブタブを追加（`view: 'edit' | 'overview'`）
  - `selectedKeys`（当週セル選択）と `externalSlots`（当週外スロット）を状態として管理
  - 保存時に `externalSlots` とグリッド選択分をマージして PUT することでデータロスを防止
  - `referenceDate` を `useMemo(() => new Date(), [])` で固定し、長時間利用時の週ずれを防止
  - 保存ボタンは `view === 'edit'` 時のみ表示
- `ScheduleTab.test.tsx` にテスト2件を追加
  - 「みんなの予定」タブクリックで保存ボタンが非表示になることを確認
  - 当週外スロットが保存時にマージされることを確認
  - `ScheduleSlot` 型インポートを追加

### 理由
週次グリッドUIへの移行に伴い、ScheduleTab の状態管理を WeeklyGrid 統合型に刷新し、当週外データのロスを防ぐマージ保存ロジックを実装するため
