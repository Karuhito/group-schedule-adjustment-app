# CLAUDE.md

このファイルはClaude Codeがプロジェクトを理解するための設定ファイルです。
詳細な仕様は `docs/spec.md` を参照すること。

---

## プロジェクト概要

**グループスケジュール調整アプリ**

Discordユーザー向けに、グループメンバーの空き時間を共有・可視化するWebアプリ。
「@everyoneしても誰も来ない」問題を解決するために、誰がいつ暇かを一目で確認できる。

---

## 技術スタック

| レイヤー | 技術 |
|--------|------|
| フロントエンド | React + TypeScript + Tailwind CSS |
| バックエンド | Python + FastAPI |
| データベース | Supabase（PostgreSQL） |
| 認証 | Discord OAuth2 |
| フロントデプロイ | Vercel |
| バックエンドデプロイ | Fly.io |

---

## コンテキスト管理

コンテキストの使用率が50%を超えたら、作業を続ける前に必ずユーザーに `/compact` の実行を促すこと。

---

## 変更ログのルール

ファイルを追加・編集・削除したら、必ず `docs/changelog/YYYY-MM-DD.md` に変更内容を記録すること。

### フォーマット

```markdown
## HH:MM - 変更の概要

**種別**: 追加 / 編集 / 削除
**対象ファイル**: `path/to/file.py`

### 変更内容
- 何をしたか（箇条書き）

### 理由
なぜその変更をしたか
```

### ルール
- 1日1ファイル（`2025-01-01.md` のように日付でまとめる）
- 複数ファイルを同時に変更した場合は1つのエントリにまとめてOK
- 軽微な修正（タイポ修正など）も必ず記録する

---

## コミットメッセージのルール

コミットメッセージは必ず日本語で書くこと。

### フォーマット
```
[YYYY-MM-DD HH:MM] 種別: 変更の概要

- 詳細1
- 詳細2
```

### 種別一覧
| 種別 | 使いどき |
|-----|---------|
| 追加 | 新しいファイル・機能を追加したとき |
| 修正 | バグ修正・既存機能の変更 |
| 削除 | ファイル・機能を削除したとき |
| リファクタ | 動作を変えずにコードを整理したとき |
| 設定 | 環境設定・ライブラリ追加など |

### 例
```
[2025-01-01 14:30] 追加: Discord OAuthログイン機能を実装

- /auth/discord エンドポイントを追加
- JWTトークン発行処理を実装
```

---

## コーディングルール

- **バックエンド**: 型ヒントを必ず書く。スキーマはPydanticで定義する
- **フロントエンド**: `any`型は使わない。コンポーネントはなるべく小さく分割する
- **API通信**: `frontend/src/api/client.ts` に集約する
- **エラーハンドリング**: APIエラーは必ずキャッチしてユーザーに表示する